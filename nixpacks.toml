# nixpacks.toml — Railway build config
# Python3 + pip3 + ffmpeg are all pre-installed on Railway's Ubuntu image

[phases.install]
cmds = [
  "npm install",
  "pip3 install -U yt-dlp"
]

[start]
cmd = "node server.js"
